$("#profile_id").submit(function (e) { /* the id form_id is submitted, it will run this function*/
    e.preventDefault(); /* this will prevent the entrie page from refreshing, when a form is submitted */


var spot= document.getElementById("profile-name").value;
var tap = document.getElementById("profile-image").value;
var speed= document.getElementById("profile-bio").value;

});